app-id: com.github.dimkr.gplaces
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
rename-icon: gplaces
rename-desktop-file: gplaces.desktop
command: gplaces
modules:
  - name: gplaces
    buildsystem: simple
    build-commands:
      - make PREFIX=/app CONFDIR=/app/etc WITH_FLATPAK_SPAWN=1 install
    sources:
      - type: git
        url: https://github.com/dimkr/gplaces
        tag: v0.20.1
        commit: 50c1c9aaa0126dd63950de39901318c4fcfb2ad6
finish-args:
  - --share=network
  - --filesystem=xdg-download
  - --talk-name=org.freedesktop.Flatpak
